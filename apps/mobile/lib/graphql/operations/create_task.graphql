mutation CreateTask(
  $title: String!
  $description: String
  $priority: TaskPriority
  $dueAt: Time
) {
  createTask(
    title: $title
    description: $description
    priority: $priority
    dueAt: $dueAt
  ) {
    id
    title
    state
  }
}
