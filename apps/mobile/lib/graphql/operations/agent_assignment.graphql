query AgentAssignment($id: ID!) {
  agentAssignment(id: $id) {
    id
    stage
    ask
    gatheredContext
    createdAt
    task {
      id
      shortId
      title
      description
      state
      currentStage
    }
  }
}
