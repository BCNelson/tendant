mutation PairDevice($password: String!, $displayName: String!) {
  pairDevice(password: $password, displayName: $displayName) {
    session {
      id
      displayName
      createdAt
      lastSeenAt
    }
    token
  }
}
