// Package agent implements the one trusted agent runner — a plan→act→observe
// loop parameterized by AgentConfig rows from the catalog. A "specialist" is a
// config over this runner, not a separate code path.
package agent
